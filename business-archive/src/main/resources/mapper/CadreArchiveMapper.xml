<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruoyi.business.archive.dal.mapper.CadreArchiveMapper">
    <select id="countForDeptId" resultType="com.ruoyi.business.common.domain.dto.CountDTO">
        select dept_id idNumber, count(*) count
        from bus_cadre_archive
        where deleted = 0
        group by dept_id
    </select>

    <select id="countByDeptId" resultType="com.ruoyi.business.common.domain.dto.CountDTO">
        select dept_id idNumber, count(*) count
        from bus_cadre_archive
        where deleted = 0
          and dept_id in
        <foreach collection="deptIdList" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
        group by dept_id
    </select>
</mapper>