<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ruoyi.business.official.doc.dal.mapper.DocDeptMapper">

    <select id="page" resultType="com.ruoyi.business.official.doc.dal.dos.DocDept">
        SELECT *
        FROM bus_doc_dept where deleted = 0
        <if test="req.deptName != null and req.deptName != ''">
            AND (dept_name LIKE CONCAT('%', #{req.deptName}, '%') OR official_name LIKE CONCAT('%', #{req.deptName}, '%'))
        </if>
        <if test="req.deptType != null">
            AND dept_type = #{req.deptType}
        </if>
        <if test="req.deptLevel != null and req.deptLevel != ''">
            AND dept_level = #{req.deptLevel}
        </if>
        ORDER BY dept_type, sort, create_time DESC
    </select>

</mapper>
